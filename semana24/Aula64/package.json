{
  "name": "aula64",
  "version": "1.0.0",
  "description": "Resolvendo Problemas (Curie I)",
  "main": "index.js",
  "scripts": {
    "start": "clear && tsnd --transpile-only --ignore-watch node_modules src/index.ts",
    "exercise1": "clear && tsnd --transpile-only --ignore-watch node_modules src/exercise1",
    "exercise2": "clear && tsnd --transpile-only --ignore-watch node_modules src/exercise2",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "Bruno Silva",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^15.3.0",
    "ts-node-dev": "^1.1.6",
    "typescript": "^4.2.4"
  }
}
